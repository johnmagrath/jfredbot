{
  "name": "world-cup-today",
  "version": "1.0.0",
  "private": true,
  "description": "Premier League scores viewer — static web app for Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
